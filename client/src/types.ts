export type Link={id:string;slug:string;destinationUrl:string;clickCap:number|null;clickCount:number;disabled:boolean;status:'active'|'disabled'|'capped';createdAt:string;updatedAt:string};
export type Stats={date:string;clicks:number};
